% SPDX-License-Identifier: GFDL-1.3-or-later
% Copyright The WP43 and C47 Authors
%
% AN0023 -- Elliptic integrals and Jacobi elliptic functions on the C47/R47, and the k versus m
% modulus convention that makes the same calculation look like two different answers.
% Compiled with lualatex, and with the shared design language in ../style/c47appnote.sty. Build with
%
%   TEXINPUTS=../style: lualatex AN-0023-C47-R47-Application-Note-2026-04-17-WGR-elliptic.ltx
%
% twice, so the table of contents, the page count and the cross references settle.

\documentclass[canadian]{article}
\usepackage{c47appnote}

% The ellipse diagram marks the parametric angle, which needs the TikZ angle machinery. The shared
% package loads only calc, since no other note draws a geometric construction.
\usetikzlibrary{angles,quotes}

% \mo marks the modulus parameter under discussion, which is the whole subject of the note. The
% circulated draft marked it in red; the accent is reserved for warnings, so the emphasis is weight.
\newcommand{\mo}[1]{\symbf{#1}}

\notenumber{023}
\noteshorttitle{Elliptic Functions}
\notedate{2026-04-17}
\notetitle{Elliptic Functions and Integrals}

\begin{document}

\notehead

    This document describes how elliptic integrals and functions are used in the C47/R47 calculator.

    In some cases, elliptic function parameters are used differently depending on which
    book or software package you are looking at.  This is a matter of slight differences in
    terminology and notation across the elliptic function literature.  Especially notable is
    the modulus parameter, interpreted differently and with the letter $k$ or $m$, with different
    meaning.  When comparing computations from one place or another, the answers may appear to
    disagree, but only because the parameter was used differently.

    This document attempts to explain those differences so that you can get consistent
    results.  If parameters are mapped properly from one usage to another, the answers
    should come out well.

    Numerical results are given to 5 digits to the right of the decimal point where possible,
    for consistency in comparisons.  The text \cite{Lawden01} uses that precision,
    which is easily set on the C47/R47 with \fkey{DISP}, \mkey{FIX}, \key{5}.  Every screen shown in
    this note was captured after that one setting, so the display groups the five decimals as
    \screen{0.198 72} rather than running them together.

    Two of the functions here take an angle, and they read it in whatever angular mode the calculator
    is in.  The worked examples in the body are in radians, so set \fkey{MODE}, \mkey{RAD} before
    following them; the comparison tables in Section \ref{jacobiEllipFns} are the exception and are in
    degrees, which is the factory setting.  Getting this wrong does not produce an error, only a wrong
    answer, so it is worth checking the small \reg{r} in the status bar before you start.

    The elliptic functions live in one menu of their own.  Open the extended-functions menu with
    \gkey{X.FN} and page to its second page, where the first key on the blue row is \gmkey{Ellipt}.
    The menu runs to three pages and the paging wraps, so \key{$\uparrow$} gets there in one press
    where \key{$\downarrow$} would take you to the third page instead.  Everything this note uses is
    on the single page that \menu{Ellipt} opens, and the keystrokes below name each function by the
    label the screen prints above its soft key.  Figure \ref{fig:ellipt} on page
    \pageref{fig:ellipt} is that page as the calculator draws it.

    \begin{warning}
    The elliptic functions are a build option, and on the DM42 and DM42n only firmware package 2
    carries them.  Packages 1, 3 and 4 leave \menu{Ellipt} and every function in it out, and the
    menu labels are struck through where they would have been.  If \menu{Ellipt} is struck out on
    your calculator, you are running a package without the elliptic option rather than doing
    anything wrong.
    \end{warning}

\tableofcontents

\vspace{2em}

\begin{revisions}
    \revision{1.0}{2026-04-17}{First published, against firmware 0.109.03.02, with Annex 1, a
                               worked tutorial by Jaco Mostert, appended to the published PDF.}
    \revision{1.1}{2026-07-30}{Moved to the shared design language in
                               \file{sources/style/c47appnote.sty}, and the date pinned rather than
                               taken from the build clock.  Keystrokes are drawn in the house
                               notation and every one of them was replayed against the simulator:
                               that added the seven screen captures and corrected three steps, the
                               menu paging key, the repeat of \cmd{MVAR}, and the angular mode the
                               $\phi$ examples need.  Every numerical result in the note was
                               reproduced unchanged.}
\end{revisions}

% ===================================================================================================
\section{Basics of Elliptical Functions and Integrals}

    What are elliptic functions and integrals?

    In some ways they are just solutions to differential equations that have no apparent bearing
    on ellipses at all.

    Here we will explore those issues and show simple examples of computations with elliptic functions
    that you can try on a C47 or R47 calculator, or in Mathematica.

    \subsection{Review:  Basic Geometry of an Ellipse}

        Many people studied conic sections in high school.  See \cite{Rel01} for example.
        A conic section is a geometrical
        idea from ancient Greek mathematics.  Certain curves can be represented as a conic
        section geometrically combined with analytical (Cartesian) geometry.  Table
        \ref{tab:conics} gives the examples.

    \begin{ntable}{@{}l c c@{}}{Examples of conic sections.  The ellipse is the one case with an
             eccentricity that is neither fixed nor unbounded, and that eccentricity is the modulus
             the elliptic functions take.}{tab:conics}
        \hd{Name} & \hd{Equation} & \hd{Eccentricity, $e$} \\ \midrule
        Circle & $x^2+y^2=r^2$ & 0 \\
        Parabola & $y^2=4 a x$ & 1 \\
        Ellipse & $\frac{x^2}{a^2}+\frac{y^2}{b^2}=1$ & $\sqrt{1-\frac{b^2}{a^2}}$ \\
        Hyperbola & $\frac{x^2}{a^2}-\frac{y^2}{b^2}=1$ & $\sqrt{1+\frac{b^2}{a^2}}$ \\
    \end{ntable}

    Since we are interested in ellipses here, let us note that the ellipse has eccentricity
    $e \in [0,1)$.  In the special case where an ellipse has eccentricity 0, it is actually
    a circle.

    \begin{figure}[ht]
        \centering
        \begin{tikzpicture}
            \draw[draw=keyink!45, line width=0.4pt, ->] (-2.7,0) -- (2.7,0)
                node[right, text=soft, font=\small] {$x$};
            \draw[draw=keyink!45, line width=0.4pt, ->] (0,-1.6) -- (0,1.6)
                node[above, text=soft, font=\small] {$y$};
            \draw[soft, line width=0.7pt] (30:2cm and 1cm) arc (30:360:2cm and 1cm);
            \draw[keyink, line width=1.4pt] (0:2cm and 1cm) arc (0:30:2cm and 1cm);
            % The semi-axis guides sit on the axes, so they carry a heavier dash than the axis lines
            % do or they disappear into them.
            \draw[keyink, dashed, line width=0.6pt] (0,-0.16) -- (2,-0.16)
                node[midway, below, text=keyink, font=\small] {$a$};
            \draw[keyink, dashed, line width=0.6pt] (-0.16,0) -- (-0.16,1)
                node[midway, left, text=keyink, font=\small] {$b$};
            \fill[keyink] (0,0) circle (1.6pt);
            \node[text=soft, font=\small] at (-0.26,-0.3) {$0$};
            \coordinate(O) at (0,0);
            \coordinate(A) at (2,0);
            \coordinate(B) at (30:2cm and 1cm);
            \draw [keyink, line width=0.7pt] (O) -- (B);
            \pic[draw=keyink, line width=0.5pt, ->, "$\phi$", angle radius=9mm,
                 angle eccentricity=1.28, text=keyink]{angle = A--O--B};
        \end{tikzpicture}
        \caption{The ellipse $x^2/a^2 + y^2/b^2 = 1$ with semi-axes $a$ and $b$.  The heavy arc is
                 the piece swept from the $x$ axis out to the parametric angle $\phi$, and its
                 length is what the incomplete elliptic integral of the second kind returns.}
        \label{fig:ellipse}
    \end{figure}

    Besides the equation given above for an ellipse, it can also be represented parametrically,
    with $\phi$ the angle drawn in Figure \ref{fig:ellipse}:

    \[x = a \cos \theta,\ y = b \sin \theta,\ \theta \in [0, 2 \pi)\]

% ===================================================================================================
\section{A Problem to Solve with an Ellipse:  Arc Length and Inverse}

    One sees online comments, e.g. \cite{SE01}, about elliptic integrals and functions
    deriving from arc length problems, but actual examples are hard to find.
    Most of the applications for elliptic integrals
    and functions go back to problems from specialized differential equations or engineering
    problems.

    I persisted in this question and found answers that tie back to the functions
    in the C47/R47 calculator.  Here are my findings.

    First, there is the problem of notation and parameter names.
    This is discussed in \cite{jdcook01}.  An elliptic function like
    $sn(u,m)$ can be described with either $m$ or $k$ given as the second argument, and
    sometimes the second argument is not mentioned at all.

    Similarly, an elliptic integral can be described as $K(m)$ or $F(\phi,k)$ or $F(\phi,m)$,
    with apparently $k$ and $m$ being different things.  And then, their relationship to
    geometric parameters that we know from simple conic descriptions, like $a$ or $b$ or
    $e$, is not obvious.

    The simple answer is, that when you see $k$ as a parameter, it is called the ``modulus'',
    but actually it is equal to the eccentricity $e$ as seen in elementary conic section
    terminology.  Some sources instead refer to $m$ as the modulus.

    However, though $k$ is often favoured in mathematical treatments, you often
    see in software such as the C47/R47 or Mathematica a parameter called $m$.  Actually, this is
    almost the same thing:  $m=k^2=e^2$.  This $m$ parameter is just the square of
    eccentricity.

    Now, a typical second year calculus textbook for engineering students teaches how to
    calculate arc length in two- or three- dimensional Euclidean space, using integration.
    The Olmsted book, \cite{Olmsted01}, does a good job of this.

    To paraphrase \cite{Olmsted01}:
    If $C$ is an arc with parametrization functions $x = f(t), y = g(t)$, with continuous
    derivatives on $[a,b]$, then $C$ is rectifiable with length

    \[L = \int_{a}^{b}{\sqrt{(f'(t))^2+(g'(t))^2}\, d t}\]

    As an example of this, for the ellipse described above, we get (with a little
    rearrangement):

    The total arc length of the ellipse is \cite{Olmsted01}

    \[4 a \int_{0}^{\pi / 2}{\sqrt{1 - k^2 \sin^2{\theta}}\, d \theta}\]

    This leads to the following.  Incomplete elliptic integral of the second kind:

    \[E(\phi,\mo{m}) = \int_{0}^{\phi}{\sqrt{1 - \mo{m} \sin^2{\theta}}\, d \theta}\]

    Complete elliptic integral of the second kind:

    \[E(\mo{m}) = \int_{0}^{\pi / 2}{\sqrt{1 - \mo{m} \sin^2{\theta}}\, d \theta}\]

    Confusingly, in different parts of the literature, you will see this version instead.
    Incomplete elliptic integral of the second kind:

    \[E(\phi,\mo{k}) = \int_{0}^{\phi}{\sqrt{1 - \mo{k}^2 \sin^2{\theta}}\, d \theta}\]

    Complete elliptic integral of the second kind:

    \[E(\mo{k}) = \int_{0}^{\pi / 2}{\sqrt{1 - \mo{k}^2 \sin^2{\theta}}\, d \theta}\]

    See also \cite{WI01}.

    Just understand that $k$ is eccentricity of the ellipse, $m=k^2$, and that
    for both you have the restriction $k \in [0,1), m \in [0,1)$.

    So now we have an understandable application if you followed all the bits about
    conic sections, eccentricity, and arc length.  We simply want to take an ellipse
    and calculate its arc length.  The incomplete elliptic integral of the
    second kind does this for arbitrary angle $\phi$, meaning we start at angle
    $0$ and rotate counter-clockwise to $\phi$, measuring arc length as we go.
    The complete elliptic integral of the second kind calculates
    the perimeter of the ellipse.

    Let us take an example and run it through both Mathematica and the C47/R47.

    Take $a=5, b=1$, so we have the ellipse $\frac{x^2}{5^2}+\frac{y^2}{1}=1$.

    We have eccentricity $k=e=\sqrt{1-\frac{b^2}{a^2}}=\sqrt{24/25}$.  Both Mathematica
    and the C47/R47 use a parameter named $m$, so we use modulus $m=.96$.

    \begin{tip}
    The calculator will do that step for you.  \gmkey{a,b$\rightarrow$m} on the blue row of
    \menu{Ellipt} takes the two semi-axes off the stack and returns the modulus directly, so the
    $m$ for this ellipse is \key{5}\quad\key{ENTER}\quad\key{1}\quad\gmkey{a,b$\rightarrow$m}.  It
    sorts the two axes itself, so the order you enter them in does not matter.
    \end{tip}

    In Mathematica, we can compute this:

\begin{lstlisting}
EllipticE[.96]

--> 1.05050
\end{lstlisting}

    As a sanity check, we can also compute:

\begin{lstlisting}
Integrate[Sqrt[1 - .96 (Sin[t])^2], {t, 0, Pi/2}]

--> 1.05050
\end{lstlisting}

    Also:

\begin{lstlisting}
ArcLength[{5 Sin[t], Cos[t]}, {t, 0, Pi/2}]

--> 5.25251
\end{lstlisting}

    which is $5$ times the value above, also confirming our correctness.

    If we want to calculate the partial arc length, say for an angle $\phi$ of $.2$ radians
    and the same modulus, we run

\begin{lstlisting}
EllipticE[.2, .96]

--> 0.19872
\end{lstlisting}

    On the C47/R47, we do this:

    \seq{\key{.}\key{9}\key{6}\quad\mkey{E(m)}}{X:\ \ 1.050 50}

    For the partial angle, in radians:

    \seq{\key{.}\key{9}\key{6}\quad\key{ENTER}\quad\key{.}\key{2}\quad\fmkey{E($\phi$,m)}}{X:\ \ 0.198 72}

    \shotpair{02-em}{03-ephim}{Left, the perimeter parameter: \mkey{E(m)} at $m=.96$ leaves
    \screen{1.050 50}, matching Mathematica.  Right, the partial arc at $\phi=.2$, which needs radian
    mode: the \reg{r} in the status bar has replaced the degree sign, and the answer is
    \screen{0.198 72}.  The menu stays open under the result, so the next function is one key away.}

    This confirms that elliptic integrals of the second kind give correct arc length
    calculations.

    \begin{note}
    The complete integral is on the menu's first row and the incomplete one is on the orange row
    above it, so \mkey{E(m)} is a plain soft key and \fmkey{E($\phi$,m)} takes a shift first.  The
    same split holds for the first kind, \mkey{K(m)} against \fmkey{F($\phi$,m)}.  The complete
    forms take one argument and the incomplete forms take two, $m$ in \reg{Y} and the angle in
    \reg{X}.
    \end{note}

    Now let us look at the inverse problem.  For a given modulus and a given arc length,
    we want to know what angle $\phi$ produces that arc length.

    Unfortunately, my researches show that there is no such function in either the mathematical literature or the
    software packages of Mathematica or C47/R47.  We have to resort to other methods such as root
    solving.  See \cite{SE03} for more on this issue.

    In Mathematica, try this:

\begin{lstlisting}
FindRoot[EllipticE[x, .96] == .19872, {x, .5}]

--> 0.20000
\end{lstlisting}

    Correct answer.

    \needspace{12\baselineskip}
    In the C47/R47, enter a program:

    \prog{0001: LBL 'EllE'\\0002: \ \ MVAR 'x'\\0003: \ \ MVAR 'm'\\0004: \ \ MVAR 'u'\\0005: \ \ RCL 'm'\\0006: \ \ RCL 'x'\\0007: \ \ E($\phi$,m)\\0008: \ \ RCL− 'u'\\0009: END}

    To key it in, open a new program with \key{XEQ}\,\key{.}\,\key{.} and switch to program-entry
    mode with \fkey{PRGM}.  Start the label with \gkey{LBL}, then \fkey{$\alpha$}, type
    \key{E}\,\key{l}\,\key{l}\,\key{E}, and \key{ENTER}.  Declare the first variable next:
    \mkey{P.FN1}, then \mkey{P.FN2}, then \mkey{MVAR}, then \fkey{$\alpha$}\,\key{x}\,\key{ENTER}.
    The menu stays on \menu{P.FN2} afterwards, so the second and third variables are just
    \mkey{MVAR}\,\fkey{$\alpha$}\,\key{m}\,\key{ENTER} and \mkey{MVAR}\,\fkey{$\alpha$}\,\key{u}\,\key{ENTER}.
    Then key the body: \key{RCL}\,\fkey{$\alpha$}\,\key{m}\,\key{ENTER},
    \key{RCL}\,\fkey{$\alpha$}\,\key{x}\,\key{ENTER}, then \gkey{X.FN}\,\key{$\uparrow$} to reach
    the second page of the extended-functions menu, \gmkey{Ellipt}, \fmkey{E($\phi$,m)}, and finally
    \key{RCL}\,\key{$-$}\,\fkey{$\alpha$}\,\key{u}\,\key{ENTER}.  Leave program-entry mode with
    \fkey{PRGM}.  Application Note~29 \cite{AN0029} walks through program entry from the beginning
    if any of that is unfamiliar.

    \shot{04-elle}{\cmd{EllE} keyed in, seen from the top: nine steps in 38 bytes.  The listing
    continues past \cmd{RCL} \vr{x} with \fmkey{E($\phi$,m)} and \cmd{RCL$-$} \vr{u}, and the
    \menu{Ellipt} menu is still open underneath because the last function was taken from it.  The
    three \cmd{MVAR} declarations are what let the solver ask for $m$ and $u$ and solve for $x$.}

    \begin{note}
    \cmd{MVAR} is not on \fkey{ADV}, which is where most readers look for it.  In program-entry mode
    it is two pages into the program-functions menu already on the screen: \mkey{P.FN1} on soft key
    6, \mkey{P.FN2} on soft key 6 again, then \mkey{MVAR}.  Declaring a variable with \cmd{MVAR} is
    what lets the solver ask you for it afterwards.
    \end{note}

    If you run this program \cmd{EllE} under \fkey{ADV}, \mkey{SOLVE}
    with a modulus of $.96$ and a $u$ value of $.19872$, you will get an answer
    of $0.20000$.  Naming the program puts its three declared variables on the soft keys in the order
    they were declared, so \mkey{m} takes the modulus, \mkey{u} takes the arc length, and \mkey{x} is
    the one to solve for:

    \seq{\key{.}\key{9}\key{6}\ \ \mkey{m}\quad\key{.}\key{1}\key{9}\key{8}\key{7}\key{2}\ \
         \mkey{u}\quad\mkey{x}}{x:\ \ 0.200 00}

    \shot{05-solve}{The angle recovered, \screen{0.200 00}, with the star on the \mkey{x} soft key
    marking the variable that was solved for.  Run this one in degrees by mistake and it returns
    \screen{11.458 99}, which is the same answer in the other unit and no error at all.}

    This accounts for $E(m)$ and $E(\phi,m)$ on the C47/R47, which are the Elliptic Integrals of the Second Kind.

% ===================================================================================================
\section{Elliptic Integrals of the First Kind}

    The integrals of the first kind are $K(m)$ (complete) and $F(\phi, m)$ (incomplete).

    Equations:

    \[F(\phi, m) = \int_{0}^{\phi}{\frac{d \theta}{\sqrt{1-m \sin^2{\theta}}}}\]

    \[K(m) = \int_{0}^{\pi/2}{\frac{d \theta}{\sqrt{1-m \sin^2{\theta}}}}\]

    These also have a geometric interpretation, from Complex Analysis.  On constructing a conformal
    map from an ellipse to a unit circle, the elliptic integral of the first kind
    tells you the angular displacement needed
    to map arc length in the ellipse to the corresponding point in the circle \cite{SE02}.

    In this case, there is an inverse mapping.  $am(x)$, described in
    Section \ref{jacobiEllipFns}, is the inverse
    of the incomplete elliptic integral of the first kind.

% ===================================================================================================
\section{Elliptic Integrals of the Third Kind and Jacobi Zeta}

    $\Pi(n,m)$ is the incomplete Elliptic Integral of the third kind.  It has applications in Physics,
    e.g. electrostatic potential.

    In Mathematica, use \cmd{EllipticPi[n,m]}.  On the C47/R47, use \mkey{$\Pi$(n,m)}.

    Comparison of values from Mathematica and the C47/R47:

\begin{lstlisting}
EllipticPi[0.4,0.6]

--> 2.59092
\end{lstlisting}

    \seq{\key{.}\key{6}\quad\key{ENTER}\quad\key{.}\key{4}\quad\mkey{$\Pi$(n,m)}}{X:\ \ 2.590 92}

    The Jacobi Zeta function is found in Mathematica as \cmd{JacobiZeta[$\phi$,m]} and on
    the C47/R47 as \fmkey{Z($\phi$,m)}.  It has applications in physics and in interpretation of
    elliptic arc compared with circle.

    Mathematica example:

\begin{lstlisting}
JacobiZeta[2,.5]

--> -0.11777
\end{lstlisting}

    C47/R47 example:

    \seq{\key{.}\key{5}\quad\key{ENTER}\quad\key{2}\quad\fmkey{Z($\phi$,m)}}{X:\ \ -0.117 77}

    These two match.

% ===================================================================================================
\section{Jacobi Elliptic Functions} \label{jacobiEllipFns}

    As a foundation to these functions, there are first the four Theta functions.  These arise as solutions
    to a partial differential equation related to heat diffusion over a slab:

    \[\kappa \nabla^{2} \theta = \frac{d \theta}{d t}\]

    These give rise to functions
    $\theta_{1}(z,q), \theta_{2}(z,q), \theta_{3}(z,q), \theta_{4}(z,q)$.

    The C47/R47 calculator does not implement these Theta functions, but Mathematica does:
    \cmd{EllipticTheta}\allowbreak\cmd{[a,u,q]}.
    These are used for the theory behind the following Elliptic functions, which also have applications in
    the Elliptic integrals.

    $sn(u,m), cn(u,m), dn(u,m)$.  These are rendered in Mathematica as

\begin{lstlisting}
JacobiSN[u,m]
JacobiCN[u,m]
JacobiDN[u,m]
\end{lstlisting}

    These functions have some interesting identities.  For example:

    \[sn(u,m)^2+cn(u,m)^2=1\]

    $sn(u,0)=\sin(u)$ for $u$ in radians.

    $cn(u,0)=\cos(u)$ for $u$ in radians.

    We can test these.  The \cmd{sn} is unaffected by the angular mode, but the \key{SIN} it is being
    compared against is not, so this one needs radian mode, \fkey{MODE}, \mkey{RAD}:

    \keys{\key{0}\quad\key{ENTER}\quad\key{.}\key{2}\quad\mkey{sn(u,m)}\quad\key{.}\key{2}\quad\key{SIN}}

    puts matching values in the \reg{X} and \reg{Y} registers:

    \prog{Y:\ \ 0.19867\\X:\ \ 0.19867}

    As an example of understanding how to use parameters, the book \cite{Lawden01} shows in
    Table C that for $k=0.5$ and $x=0.8$, $sn(x,k)=0.70421$.

    However, this calculation on the C47/R47:

    \seq{\key{.}\key{5}\quad\key{ENTER}\quad\key{.}\key{8}\quad\mkey{sn(u,m)}}{X:\ \ 0.690 93}

    produces the number $.69093$:  an apparent mismatch.

    Is this wrong?  NO.  The calculator got a different answer from the book, but
    both the calculator and the book are right.  WHAT?

    Yes, explaining this issue clearly is one of the main motivations
    for writing this document.  The answer is in the difference in parameter
    interpretation.  At the risk of being too verbose, I \textbf{must} explain
    this point.

    Earlier in this document, I used a lot of space to explain the $k$ parameter
    versus the $m$ parameter.  Some books or software packages expect $k$ as the
    modulus parameter, and some expect $m$ as the modulus parameter.  As explained
    earlier, $m=k^2$.

    Where the book's table has $sn(u,k)$ with $k=0.5$, the calculator wants
    to run $sn(u,m)$ and we have to square the $k$ parameter to get the $m$
    parameter.

    Knowing that $m=k^2$, we
    can recalculate on the C47/R47 this way:

    \seq{\key{.}\key{5}\quad\fkey{$x^{2}$}\quad\key{.}\key{8}\quad\mkey{sn(u,m)}}{X:\ \ 0.704 21}

    and get the matching value of $0.70421$.

    \shotpair{06-snk}{07-snm}{The whole of the note in two screens.  Left, the book's $k=0.5$ handed
    straight to \mkey{sn(u,m)}, which reads it as $m$ and returns \screen{0.690 93}.  Right, the same
    $k$ squared first, which is the $m$ the function was asking for, returning the book's
    \screen{0.704 21}.  Neither screen is wrong; they answer two different questions.}

    \begin{tip}
    \fmkey{k$\rightarrow$m} on the orange row of \menu{Ellipt} is that squaring step, named for what
    it is, and \fmkey{m$\rightarrow$k} takes you back.  Reaching for it instead of \fkey{$x^{2}$}
    says in the keystroke itself which convention you are converting from, which is worth something
    in a program you will read again in a year.
    \end{tip}

    If this sounds strange, think of trig functions.  The result of the sine function
    depends on whether the
    parameter is interpreted as being in degrees or radians or other units.
    Accordingly, you might have to do a conversion between degrees and radians in
    some cases to make the sine function give the expected result.

    Here, the result
    of $sn()$ depends on whether the second parameter is interpreted as the eccentricity
    or the square of the eccentricity of the ellipse.  Accordingly, you might have to square
    a parameter to make it compatible with the function you are calling.

    Table \ref{tab:lawdenjacobi} gives more examples comparing $sn()$ calculations with tables in
    the back of the \cite{Lawden01} textbook.  The Jacobi functions take an argument rather than an
    angle, so these do not depend on the angular mode; the elliptic integrals in Table
    \ref{tab:lawdenint} do, and are in degrees.

    \begin{ntable}{@{}c c c c c c c c@{}}{Lawden comparison examples, Jacobi functions.  The first
             three result columns are the book's, at modulus $k$; the last three are the
             calculator's, at modulus $m=k^2$.  They agree once the parameter is
             converted.}{tab:lawdenjacobi}
        \hd{$k$} & \hd{$x$} & \hd{$sn\,x$} & \hd{$cn\,x$} & \hd{$dn\,x$} &
        \hd{$sn(u,m)$} & \hd{$cn(u,m)$} & \hd{$dn(u,m)$} \\ \midrule
        .5 & .8 & .70421 & .70999 & .93596 & .70421 & .70999 & .93596 \\
        .6 & 1.3 & .93416 & .35686 & .82816 & .93416 & .35686 & .82816 \\
    \end{ntable}

    Incomplete elliptic integrals using degrees are in Table \ref{tab:lawdenint}.

    \begin{ntable}{@{}c c c c c c@{}}{Lawden comparison examples, incomplete elliptic integrals.
             Lawden's $F$ and $D$ against the calculator's $F$ and $E$, with the angle in
             degrees.}{tab:lawdenint}
        \hd{$k$} & \hd{$\phi^{\circ}$} & \hd{Lawden $F(\phi,k)$} & \hd{$F(\phi,k)$} &
        \hd{Lawden $D(\phi,k)$} & \hd{$E(\phi,k)$} \\ \midrule
        .4 & 36 & .63459 & .63459 & .62215 & .62215 \\
        .6 & 65 & 1.21431 & 1.21431 & 1.06311 & 1.06311 \\
    \end{ntable}

    Apart from this issue for $sn()$,
    there is also the Jacobi amplitude function, represented in the C47/R47 as \fmkey{am(u,m)}
    and in Mathematica as \cmd{JacobiAmplitude[u,m]}.
    Here, $m$ is the modulus representing
    the square of eccentricity, as before.

    This function is the inverse of the elliptic integral of the first kind.
    To test this on the C47/R47, try an example of $\phi=.1, m=.7$.  Calculation
    of elliptic integral of first kind:

    \seq{\key{.}\key{7}\quad\fkey{$x^{2}$}\quad\key{.}\key{1}\quad\fmkey{F($\phi$,m)}}{X:\ \ 0.100 08}

    Calculation of inverse with same modulus:

    \seq{\key{.}\key{7}\quad\fkey{$x^{2}$}\quad\key{.}\key{1}\key{0}\key{0}\key{0}\key{8}\quad\fmkey{am(u,m)}}{X:\ \ 0.100 00}

% ===================================================================================================
\section{Summary of C47/R47 Elliptic Functions}

    Table \ref{tab:summary} lists the C47/R47 functions this note has described, drawn as the soft
    keys that reach them on the single page of \menu{Ellipt}.  A plain key sits on the menu's first
    row; an orange one takes the f shift first, and a blue one the g shift.  The last three rows are
    the parameter conversions rather than elliptic functions, and they are on the page because that
    is where they are needed.  Figure \ref{fig:ellipt} is the page itself, for checking the table
    against.

    \begin{ntable}{@{}l l@{}}{The elliptic functions described here, and where each one sits on the
             single page of the Ellipt menu.}{tab:summary}
        \hd{Soft key} & \hd{Function} \\ \midrule
        \fmkey{am(u,m)}      & Jacobi Amplitude Function \\
        \mkey{sn(u,m)}       & Jacobi sn Function \\
        \mkey{cn(u,m)}       & Jacobi cn Function \\
        \mkey{dn(u,m)}       & Jacobi dn Function \\
        \mkey{K(m)}          & Complete Elliptic Integral of First Kind \\
        \fmkey{F($\phi$,m)}  & Incomplete Elliptic Integral of First Kind \\
        \mkey{E(m)}          & Complete Elliptic Integral of Second Kind \\
        \fmkey{E($\phi$,m)}  & Incomplete Elliptic Integral of Second Kind \\
        \mkey{$\Pi$(n,m)}    & Elliptic Integral of Third Kind \\
        \fmkey{Z($\phi$,m)}  & Jacobi Zeta \\
        \addlinespace
        \gmkey{a,b$\rightarrow$m} & Semi-axes to modulus, $m = 1 - (a/b)^2$ with $a$ the smaller \\
        \fmkey{k$\rightarrow$m}   & Modulus convention $k$ to $m$, that is $m = k^2$ \\
        \fmkey{m$\rightarrow$k}   & Modulus convention $m$ to $k$ \\
    \end{ntable}

    \shotfig{01-ellipt}{The Ellipt page, which is the whole menu: the unshifted row along the bottom,
    the f row above it, the g row above that.  The tiny S over L at the top right is the simulator's
    stack-lift indicator; hardware carries the battery gauge there.}{fig:ellipt}

    In each case except for $\Pi(n,m)$, the $u$ or $\phi$ parameter is the primary argument, and the
    $m$ parameter is the square of eccentricity of the ellipse.
    For $\Pi(n,m)$, $n$ is the characteristic and $m$ is the square of eccentricity.

% ================= KEYBOARD MAPS =================
\keyboardappendix

% ===== REFERENCES =================
\clearpage
\begingroup
\small
\begin{thebibliography}{99}
    \bibitem{Lawden01} Lawden, Derek F, \emph{Elliptic Functions and Applications}, Springer-Verlag, 1989
    \bibitem{Olmsted01} Olmsted, John M H, \emph{Advanced Calculus}, Prentice-Hall, 1961
    \bibitem{Rel01} Del Grande and Egsgard, \emph{Relations}, Gage Educational Publishing, 1972
    \bibitem{SE01}
        StackExchange: \emph{What even are elliptic functions?}.
        \url{https://math.stackexchange.com/questions/3383769/what-even-are-elliptic-functions}
        (Accessed:  April 2026).
    \bibitem{jdcook01} Cook, J. D.,
        \emph{Clearing up the confusion around Jacobi functions}
        \url{https://www.johndcook.com/blog/2018/10/12/jacobi-function-nomenclature/}
    \bibitem{SE02}
        StackExchange: \emph{Elliptic integral of the first kind as conformal mapping}.
        \url{https://math.stackexchange.com/questions/2341813/elliptic-integral-of-the-first-kind-as-conformal-mapping}
        (Accessed:  April 2026)
    \bibitem{SE03}
        StackExchange: \emph{Inverse of elliptic integral of second kind}.
        \url{https://math.stackexchange.com/questions/1123360/inverse-of-elliptic-integral-of-second-kind}
        (Accessed:  April 2026)
    \bibitem{WI01}
        Wikipedia:  Elliptic integral
        \url{https://en.wikipedia.org/wiki/Elliptic_integral}
        (Accessed:  April 2026)
    \bibitem{AN0029} C47/R47 Application Note 029, \emph{First Steps}.
\end{thebibliography}
\endgroup

% Two contributors, because the published PDF bundles Annex 1: see the filename, which carries both
% sets of initials. The .ltx here is the note alone.
\authors{
    \contributor{William Gordon Rutherdale}{}{}{This note}
    \contributor{Jaco Mostert}{}{}{Annex 1, the worked tutorial appended to the published PDF}
}

\end{document}
