diff --git a/src/c47/calcMode.c b/src/c47/calcMode.c
index f296702924d2ea6edfba9b4445e4d71a41ebb0f5..8697374dafe4e7d8174b862cd7a99b805cdbaaf1 100644
--- a/src/c47/calcMode.c
+++ b/src/c47/calcMode.c
@@ -41,6 +41,9 @@
       sprintf(tmp, "^^^^### calcModeNormal");
       jm_show_comment(tmp);
     #endif // PC_BUILD
+    if(calcMode == CM_GRAPHICS_CANVAS) {   // program-graphics: keep canvas view open during program steps; EXIT closes view.
+      return;
+    }
     calcMode = CM_NORMAL;
     if(softmenu[softmenuStack[0].softmenuId].menuItem == -MNU_ALPHA) {  //JM
       popSoftmenu();
