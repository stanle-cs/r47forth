diff --git a/src/c47/screen.c b/src/c47/screen.c
index d2ac868bd18f9f4c20608f5dc90d5652844afcc6..dc0d98ecb6a4fe1ef57bd554bdb941546595aeb9 100644
--- a/src/c47/screen.c
+++ b/src/c47/screen.c
@@ -5483,6 +5483,9 @@ static void displayLRtemporaryInformation(char *prefix1, char *prefix2, char *pr
   }
 
   void refreshRegisterLine(calcRegister_t regist) {
+    if(calcMode == CM_GRAPHICS_CANVAS) {   // program-graphics package: no register line paints over the canvas
+      return;
+    }
     _refreshRegisterLine(regist, !RESTORE_T);
   }
 
@@ -6221,6 +6224,12 @@ static void displayLRtemporaryInformation(char *prefix1, char *prefix2, char *pr
         _refreshNormalScreen();
         break;
 
+      case CM_GRAPHICS_CANVAS:   // program-graphics package: the canvas keeps its pixels
+        last_CM = calcMode;
+        pgRefreshCanvasView();
+        force_refresh(force);
+        break;
+
       case CM_LISTXY:
           doRefreshSoftMenu = false;
           displayShiftAndTamBuffer();
