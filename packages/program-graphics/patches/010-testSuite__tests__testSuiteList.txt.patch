diff --git a/src/testSuite/tests/testSuiteList.txt b/src/testSuite/tests/testSuiteList.txt
index 6bb764d1b588d13b2767c07e61837d86e4b5a0c0..272cadb10c56e43b67796d9535501b68888bd6d2 100644
--- a/src/testSuite/tests/testSuiteList.txt
+++ b/src/testSuite/tests/testSuiteList.txt
@@ -453,6 +453,11 @@ solve_cov
 cpxslv_cov
 eqslv_cov
 integrate_cov
+; program-graphics runs after the equation coverage files: its 3D drivers
+; load programs, run them through the engine protocol thousands of times,
+; and take and free a 2 KB pool block, and the equation files above depend
+; on the pool state they inherit (see the note before eq_cov).
+program_graphics
 tvm_cov
 
 ; These coverage files mutate shared state that the per-test In: reset does not
