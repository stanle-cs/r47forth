diff --git a/src/c47/ui/tam.c b/src/c47/ui/tam.c
index 97301df3e7f567f5458eb456b35859175c5a7e97..853252556976b2a064463768b89655cd57e97dae 100644
--- a/src/c47/ui/tam.c
+++ b/src/c47/ui/tam.c
@@ -2,6 +2,7 @@
 // SPDX-FileCopyrightText: Copyright The WP43 and C47 Authors
 
 #include "c47.h"
+#include "forth_dict.h"
 
     TO_QSPI const int16_t StoOperations[][2] = {
       {ITM_ADD,      ITM_STOADD},
@@ -974,6 +975,20 @@ printf("tam.value: %d\n", tam.value);
                 return;
               }
             }
+            /* forth-core H-hook: Forth fallback after label miss (DESIGN.md §4.2) */
+            {
+              uint16_t widx;
+              if (forthFindColon(buffer, &widx)) {
+                leaveTamModeIfEnabled();
+                if(calcMode == CM_PEM) {
+                  insertUserItemInProgram(tam.function, buffer);
+                }
+                else {
+                  reallyRunFunction(ITM_FCALL, widx);
+                }
+                return;
+              }
+            }
           }
           if(calcMode != CM_PEM) {
             leaveTamModeIfEnabled();
