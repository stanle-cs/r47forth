diff --git a/src/c47/ui/tam.c b/src/c47/ui/tam.c
index 97301df3e7f567f5458eb456b35859175c5a7e97..b67e61462ea18472102c83bcd774984bc2285801 100644
--- a/src/c47/ui/tam.c
+++ b/src/c47/ui/tam.c
@@ -2,6 +2,7 @@
 // SPDX-FileCopyrightText: Copyright The WP43 and C47 Authors
 
 #include "c47.h"
+#include "forth_dict.h"
 
     TO_QSPI const int16_t StoOperations[][2] = {
       {ITM_ADD,      ITM_STOADD},
@@ -974,6 +975,21 @@ printf("tam.value: %d\n", tam.value);
                 return;
               }
             }
+            /* forth-core H-hook: Forth fallback after GLOBAL label miss — never for a
+             * tam.colon (LOCAL) request (DESIGN.md §0.3/§4.2 label-kind pins) */
+            {
+              uint16_t widx;
+              if (!tam.colon && forthFindColon(buffer, &widx)) {
+                leaveTamModeIfEnabled();
+                if(calcMode == CM_PEM) {
+                  insertUserItemInProgram(tam.function, buffer);
+                }
+                else {
+                  reallyRunFunction(ITM_FCALL, widx);
+                }
+                return;
+              }
+            }
           }
           if(calcMode != CM_PEM) {
             leaveTamModeIfEnabled();
