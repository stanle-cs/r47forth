diff --git a/src/c47/bufferize.c b/src/c47/bufferize.c
index 8e3c6b1b719e05a09034908975d5cfe3f7616be3..faded1282487b78fc9f04a8bbe9ba971a76c9eeb 100644
--- a/src/c47/bufferize.c
+++ b/src/c47/bufferize.c
@@ -2,6 +2,7 @@
 // SPDX-FileCopyrightText: Copyright The WP43 and C47 Authors
 
 #include "c47.h"
+#include "forth_capture.h"
 
 bool_t delayCloseNim = false;
 
@@ -2691,6 +2692,12 @@ TO_QSPI static const numStr NumMsg[] = { { "^0" }, { "^1" }, { "^2" }, { "^3" },
 
 
   void closeAim(void) {
+    /* Nothing upstream closes an INTERACTIVE capture; a leaked FCAP_OPEN
+     * is not inert (next PEM ALPHA press toggles keys mode instead of
+     * opening literal input), so close it here on every closeAim() path. */
+    if(forthCapIsInteractive()) {
+      forthCapClose();
+    }
     calcModeNormal();
     popSoftmenu();
 
