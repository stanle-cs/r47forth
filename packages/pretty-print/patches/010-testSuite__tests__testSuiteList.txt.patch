diff --git a/src/testSuite/tests/testSuiteList.txt b/src/testSuite/tests/testSuiteList.txt
index 6bb764d1b588d13b2767c07e61837d86e4b5a0c0..a35b856487725244b61be98f76e0ecaaea37ce88 100644
--- a/src/testSuite/tests/testSuiteList.txt
+++ b/src/testSuite/tests/testSuiteList.txt
@@ -267,6 +267,7 @@ dot
 cross
 
 matrix
+pretty_print
 
 percent
 percentMRR
@@ -501,6 +502,13 @@ shortint_restore_cov
 
 serialize_cov
 
+; pretty-print: VISUAL against the real appnote-22 program file. It
+; CLEARS PROGRAM MEMORY, so it must come after every case that expects
+; preloaded programs (programs.txt) — and it is anchored HERE, not at the
+; end of the list, because the tail is forth-core's and two packages
+; appending at EOF conflict.
+pretty_visual_real
+
 graphs_cov
 
 nested_cov
