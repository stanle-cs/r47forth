diff --git a/src/c47/defines.h b/src/c47/defines.h
index 63b9c1a7692f24da093a882ffa1f3be1625bb7a8..022f1312b8348891008c3e18cadc677e129b94ef 100644
--- a/src/c47/defines.h
+++ b/src/c47/defines.h
@@ -1013,10 +1013,12 @@
 #define FLAG_PRMS                             0x806B
 #define FLAG_PINTG                            0x806C
 #define FLAG_PDIFF                            0x806D
+#define FLAG_PRETTYP                          0x8071 //50 natural-display master toggle (pretty-print package, count reserved by the claims registry)
+#define FLAG_PTLINE                           0x8072 //51 live formula on the T line (pretty-print package, default OFF)
 #define FLAG_PSHADE                           0x806E //47
 #define FLAG_SBadm                            0x806F //48
 
-#define NUMBER_OF_SYSTEM_FLAGS                 64+48 // We can have a maximum of 128 system flags
+#define NUMBER_OF_SYSTEM_FLAGS                 64+51 // The system supports at most 128 system flags. Value 51 reserves two flags for pretty-print: FLAG_PRETTYP and FLAG_PTLINE (claims registry). Both packages define the same count, and the three-way merge combines them.
 
                                                      // only used as bit count for setting change detection
 #define SETTING_AMODE                         0x0080 // current angle mode
