diff --git a/src/c47/saveRestoreBackup.c b/src/c47/saveRestoreBackup.c
index 621cd37c31bfa17f47be2cfd0282d7a88a358fa9..25cd9e68fdc99322665efeb750c5516e039ce41a 100644
--- a/src/c47/saveRestoreBackup.c
+++ b/src/c47/saveRestoreBackup.c
@@ -1478,6 +1478,11 @@ static void convertOldMatrixHeaderToNewMatrixHeader(calcRegister_t regist) {
     defineFirstDisplayedStep();
     defineCurrentProgramFromCurrentStep();
 
+    // The undo history described a machine state and lived in a pool that
+    // were both just replaced wholesale: forget it and re-arm its ring
+    // block from the restored pool.
+    undoHistoryReset();
+
     //defineCurrentLocalRegisters();
 
     //save and restore screenData is not mandatory
@@ -1502,6 +1507,7 @@ static void convertOldMatrixHeaderToNewMatrixHeader(calcRegister_t regist) {
             || calcMode == CM_FLAG_BROWSER
             || calcMode == CM_ASN_BROWSER
             || calcMode == CM_FONT_BROWSER
+            || calcMode == CM_HIST_BROWSER
             || calcMode == CM_PEM
             || calcMode == CM_PLOT_STAT
             || calcMode == CM_GRAPH
