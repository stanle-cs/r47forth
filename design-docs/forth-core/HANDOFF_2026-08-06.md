# Handoff — 2026-08-06

Where `forth-core/stage-n` stands at the end of the Stage N + audit
session, what is open, and what the next session should not have to
rediscover. Non-normative; `DESIGN.md` is authoritative.

**HEAD `b8e1da153`, 89 commits ahead of `main`. Gate green, tree clean.**

---

## 1. What landed

**Stage N — the console (`84da2ff9c..486574c78`, six packets).** The
interactive Forth capture now has a face: a transcript over the stack
area, an output channel, keys-first entry. Normative record is
`DESIGN.md` §8.4.4, with §8.4.1 / §8.4.2 / §1.3 / §5.4 amended; the
narrative is in `DESIGN-HISTORY.md`. Sim captures:
`forum/screenshots/stage-n-{1,2}-*.png`.

**The adversarial audit (`d49ff2916` onward).** A multi-reader bug hunt
adapted from `forum/`'s post-audit workflow — see §4.

**Four audit findings fixed**, each with a class test and a mutation
that reddens it: C1 (`3712ea3a9`), the C2/C3/C4/C8/C9 softmenu-row family
(`db5e5ca30`), a restore-path frame leak (`d2fcb401c`), C16
(`723361f58`).

**Cumulative numbers** (`make dmcp5r47 CUSTOM_PKG_RECONFIGURE=1`), Stage
M close → HEAD: flash 1111680 → 1114120 = **+2440 B**; ram 7844 → 8884 =
**+1040 B** (the 1024-byte view ring plus its state). Arena untouched
throughout — no stage in this range changes the dictionary.

---

## 2. Open findings

Two reports, both committed:
`AUDIT_stages-K-to-N_2026-08-06.md` (round 1, 15 confirmed) and
`AUDIT_Stage-N-console-audit-round-1-fixes_round2-2026-08-06.md`
(round 2, 7 confirmed). Each finding there carries its reaching input,
the contract it violates, and a suggested class test.

**Start here.** C17 is the only open finding that costs the owner
something *unrecoverable*.

| # | what | why it matters |
|---|---|---|
| **C17** | `forthConsoleShowSurface` retargets slot 0 on menu identity alone, never asking whether the console *owns* that frame | the owner's own FWRD frame is consumed, not merely displaced — another press does not get it back. **See §3.** |
| C18 | three callers commit the sub-mode flip, then call a function entitled to do nothing, so the row can lie about the input mode | the keypad types `Σ+` where the row says `A` |
| C19 | the ENTER error arm appends the message into a word's still-open output record | the error text can be ellipsized off the right edge |
| C5 | history recall with nothing to recall wipes the line being typed | lost work, ordinary gesture |
| C6 | `fnForthOuter` has no already-open guard — pressing FORTH inside the console discards the line with no history push | lost work |
| C7 | `.S` prints a constant `<4>` and never shows the top four levels of an 8-level stack | wrong output; N-R5 says "depth-prefixed picture of the live stack" |
| C20 | `EMIT` truncates an out-of-`int32` long integer into the accepted range | prints a wrong character instead of refusing |
| C10 | `EMIT` accepts codes whose low byte is `0x00`, writing a lone high byte into the ring | glyph corruption |
| C11 | the formatter's `dtString` arm truncates on a byte boundary | same orphan-byte class as C10 |
| C12 | the roll clamps at `count-1` rather than `count-rows` | scrolling back empties the transcript band. **Needs a ruling, not a patch — see §6.** |
| C21 | nothing in the battery can fail if the console stops suppressing the register paints | a whole feature is untested |
| C22 | the C1 class test's canaries guard a buffer no producer receives | **see §5 — this one downgrades work already done** |
| C13 | the `.`-stack-delta assertion reads a counter zeroed before it is read | worse error message, not a missing gate |
| C14 | the console's `editorTop` constants (128/67) are hand-copied from `showStringEdC47` | merge hazard; two `_Static_assert`s fix it |
| C15 | `DESIGN.md`'s override list omits `assign.c`, still names `error.c` | one documentation line |

---

## 3. C17 — read this before attempting it

Confirmed, reproduced by mutation, **and I tried to fix it and failed.**

**The defect.** Browse to FWRD through the CATALOG tree, so the *owner's*
FWRD frame is slot 0. Press FORTH: the open sees FWRD already current, so
it pushes nothing and records `homePushed = 0` — correctly meaning "slot 0
is not ours". Press f+XEQ: `forthConsoleShowSurface` takes the
`cur == -MNU_FORTH` branch and overwrites `softmenuStack[0].softmenuId`
with the ALPHA index, **without ever asking whether that frame is the
console's**. Then any line running `calcModeNormal()` (`XEQ 'CLSTK'`)
pops it, because that guard matches `-MNU_ALPHA`. EXIT, and the owner gets
the catalog level that was underneath.

Frame *count* is conserved, which is why the landed battery passes; frame
*identity* is not.

**The obvious fix is not sufficient.** Gating the retarget on
`forthCapHomePushed()` and pushing when the console does not own slot 0
**failed the two probe rows it was meant to close and also regressed
`[open, alpha, ENTER, EXIT]`**, which had been passing — the owner's row
came back as a TAM menu (`-1387`). Reverted; the tree is green without it.

I did not get to the bottom of that interaction. What I would try next:

- The push-when-not-owned path sets `homePushed = true`, after which the
  *return* leg retargets rather than pops. Trace both legs together
  rather than the outbound one alone.
- `forthConsoleRestoreSurface` runs on the ENTER path and has its own
  push-and-take-ownership branch. Two functions can now both claim
  ownership of a frame in one line's lifetime.
- Sol's framing, reached independently and worth starting from:
  *"menu ownership — not merely visible menu identity — controls the EXIT
  ladder."* The right model may be an explicit owned-frame **identity**
  (which slot, which menu) rather than a single ownership bit.

The reproducing fixture is in the round-2 report under C17: the owner
fixture is `showSoftmenu(-MNU_CATALOG); showSoftmenu(-MNU_FORTH);` and the
differential is that a plain `1 2 +` line passes where a `XEQ 'CLSTK'`
line fails.

---

## 4. The audit system

| file | what |
|---|---|
| `CODE_AUDIT.md` | the process: dimensions, reader pool, what NOT to flag, exit criterion, report template |
| `PROMPT_CODE_AUDIT.md` | the auditor brief + the refutation brief. Paste whole, or inline into a subagent prompt |
| `audit-workflow.js` | the runner: 8 blind finders → dedup → adversarial refutation → report |

Run it with `Workflow({scriptPath: 'design-docs/forth-core/audit-workflow.js', args: {subject, commits, files, date}})`.

**The out-of-family reader is Gemini**, and this is settled after twelve
runs:

```bash
agy --model gemini-3.1-pro-high --print-timeout 12m -p "$(cat packet.txt)"
```

- **Flag order decides which model answers.** `agy -p --model X` silently
  serves *Claude*. This was the order documented in `forum/DESIGN.md`, so
  forum audits run that way were Claude auditing Claude — corrected there
  too, with a standing rule to put "state your model name" at the top of
  every audit prompt and check the answer.
- **Send whole functions**, and state the orientation of any shared
  structure the reader cannot inspect (e.g. "slot 0 is the TOP"). Round 2
  produced two findings that were artifacts of a truncated packet and a
  missing orientation, and both looked exactly like good findings.
- **Small packets only.** ~3 KB works in minutes; a 13 KB packet returned
  nothing from either reader.
- **Sol via `codex` does not complete an audit.** The sandbox blocker is
  real (bubblewrap missing; `--dangerously-bypass-approvals-and-sandbox`
  clears it, owner-authorised for this WSL2 box) but the remaining
  problem is behavioural: it reads exhaustively and ignores every budget
  instruction. Full diagnosis in `CODE_AUDIT.md`.

**Exit criterion: unmet, and it will reset on every fix.** Two
consecutive rounds with no new confirmed finding, at least one
out-of-family. Round 2 is the reason to trust that rule — **four of its
seven findings were caused by round 1's fixes.**

---

## 5. Things that downgrade work already done

**C22 — the C1 class test is weaker than the session suggested.** The
DMCP target build carries **no `-fstack-protector`**. The mutation that
went red for C1 went red because of the *simulator's* protector, not
because the canary test caught it. On the device — where C1's consequence
is a reboot — that backstop does not exist. The C1 *fix* is right; the
test is not the proof it appeared to be.

**Three vacuous tests in one session, all mine, all caught by running the
mutation and none by the gate.**

1. A hammer invariant comparing a `uint8_t` against 255 — `-Wtype-limits`
   caught it.
2. The C1 class test built its short integer with `255 XEQ 'HEX'` — `HEX`
   is a `CAT_FNCT` item reached by *bare name*, so the line raised
   label-not-found, X stayed a long integer, and the arm under test was
   never reached.
3. The C16 class test performed the save/restore *in the test body* and
   asserted the bits survived — testing its own copy of the block.

The rule that caught all three, and the one to keep: **a fixture must
assert it reached the state it claims to test**, and **a fix is not
finished until the mutation reddens.** A green suite proves nothing about
a test you just wrote.

---

## 6. Decisions that are the owner's, not the implementer's

- **C12's layering.** The roll should clamp at `count - rows`, but `rows`
  is a view concept the ring module deliberately does not have. Either the
  ring learns about rows, or the view owns the clamp. Worth a ruling
  before a patch.
- **C18's catalog shape** and **R9's dead line** — the round-2 report's §7
  says it would leave both alone.
- **Whether C22 changes the C1 test.** It would have left it alone if the
  target build had a stack protector.

---

## 7. Standing items, unchanged

- **DM42n hardware pass** for stages L, M and N.
- **Merge/push** `forth-core/stage-k`, `-l`, `-m`, `-n` — 89 commits, all
  unmerged.
- **FIX-6B MR push** and the **leak-report filing** (`UPSTREAM_REPORTS_*.md`).

---

## 8. Suggested order for the next session

1. **C17** with fresh attention, starting from §3 rather than from the
   obvious fix.
2. **C18 and C19** — both are ordinary, both are on gestures an owner
   makes constantly.
3. **C21** — a whole feature with no test that can fail is worse than a
   bug with one.
4. **Round 3**, which the reset rule requires anyway. Gemini for the
   out-of-family pass, whole functions, small packets.
5. Then the DM42n pass and the merge, with a finding list short enough to
   be honest about.

---

# Second-session addendum — 2026-08-06, later the same day

**HEAD `8dc577f25`, 93 commits ahead of `main`. Gate green, tree clean.**

## What landed

- **C17 fixed** (`dad69ee16`) — §3's analysis held up: the right model
  was frame identity, not an ownership bit.  The registered frame
  carries a sentinel in its own `userMenuId` (OWNED vs BORROWED);
  `homePushed` is gone, along with both of its hand-preservation sites.
  Both out-of-family readers reviewed the DESIGN before it was coded and
  both attacks landed — the BORROW stamp and the FWRD-only fold-back
  exist because of them.  Read the banner in `forth_menu.c`.
- **C18 + C19 fixed** (`8dc577f25`) — the EXIT ladder pops overlays
  BEFORE the excursion flip, the toggle refuses under an overlay, the
  REPL reopen retargets the buried owned base at depth, and the error
  echo closes the word's open output record first.
- **The Sol driver question is settled the other way** (`89b14b738`):
  `codex` COMPLETES a self-contained packet (everything inline, empty
  dir, read-only sandbox, hard timeout) and did real design-review work.
  Repo-exploration audits remain Gemini-only.  Recipe in CODE_AUDIT.md.

## Corrections to trust

- §1's cumulative numbers stand.  This session's deltas: C17 +344 B
  flash, C18/C19 +64 B, RAM 8884 unchanged, arena untouched.
- **Measurement trap:** `make dmcp5r47 CUSTOM_PKG_RECONFIGURE=1` without
  `CUSTOM_PKG=packages/forth-core` builds STOCK firmware and reports
  plausible sizes with no error.  Tell: flash ~1090 KB instead of
  ~1114 KB.  Check: `arm-none-eabi-nm | grep -c forthConsole`.
- **Four fixtures repaired under §5's rule this session** (ladder
  rung-1, K-battery [3a] and [3(b)], and the C18 battery's own first
  draft): every one faked a state by forcing `keysMode` or hand-pushing
  rows.  The pattern to watch for in remaining tests: a fixture that
  writes capture state directly instead of driving the gesture.

## Open findings: 12

C17, C18, C19 close.  §2's table otherwise stands: **C21 next** (the
suppression battery), then round 3 — the exit criterion reset three
more times today.  C5, C6, C7, C20, C10, C11, C13, C14, C15, C22
unchanged; C12 and C18's-catalog-shape/R9 still need owner rulings (§6).


---

# Third addendum — round 3, and what it says about the process

**HEAD `e3b52e83f`. Gate green, tree clean.**

## The number that matters

Round 2: four of seven findings were caused by round 1's fixes.
**Round 3: four of four were caused by round 2's fixes.** The rate is not
falling, and every round so far has proved the same thing — a fix is new
code nobody has audited. Do not treat "the batteries are green" as
evidence about code written the same day.

## What round 3 found (all fixed, `e3b52e83f`)

- **R1, the one to internalise.** C17 moved ownership out of the capture
  object and into `softmenuStack[].userMenuId`. That was right — it is
  what made ownership survive reopen and resume. But `homePushed` was
  *never persisted* and the softmenu stack *is*, saved wholesale as a hex
  dump, so the stamp outlived its capture across a restore and poisoned
  the next session. **Class: state moved into a structure with a
  different persistence contract than the one it left.** Four of seven
  finders got this independently.
- **R2** abandoned suspensions bypass the unstamp funnel. **R3** the
  post-run surface repair was gated on the mode repair's condition, so
  `EXITALL` (a typed line — it is `CAT_FNCT|PTP_NONE`) destroyed the
  console's row invisibly. **R4** the invariant I wrote in three places
  was false; two registrations is the correct state of the alpha
  excursion over the user's own FWRD row.

## Two PLAUSIBLE carried, both needing YOUR ruling (add to §6)

- **P1** a non-ladder close leaves the console's created row standing.
  Exact pre-C17 parity, so not a regression — but now visible. Fixing it
  means giving the close funnel a pop, and the funnel runs where that may
  be wrong.
- **P2** the ALPHA acquire drops the bottom frame on a full stack.
  Matches native push behaviour, but it is a new push site.

## Read this before trusting round 3

**The refutation pass did not run** — eleven verifiers and the
synthesiser all died on usage credits. Every verdict in
`AUDIT_round3_2026-08-06.md` is the author's trace of the author's own
code, and the `design` dimension never ran. **Round 4's first job is
re-verifying round 3's four fixes with a refutation pass that actually
runs**, not new coverage.

The out-of-family half worked and is the part to keep: Gemini and Sol
both answered, both landed real findings. Sol's one bad finding was a
**packet** defect — the packet did not mention the package's
`softmenus.c` override that rebuilds the FWRD picker on every paint, and
a reader with no repository cannot know that. Third packet artefact in
three rounds: **the orientation block must name every package override
the excerpt depends on.**

## Test-work state

Five fixture defects this session, all caught by the C22 rule. The fifth
is new in kind — a wrong oracle (`forthConsoleBaseOnTop()`, whose
identity fallback answers "true" for an unstamped stack) **manufactured
three false failures**. Wrong oracles fail in both directions.

Test 11's empty-band assertion has been mis-attributed twice in commit
messages and a third story was drafted and killed by its own mutation.
It is now marked a DOCUMENTED GAP in the test body. **An assertion's
provenance is a claim, and claims get mutations too.**

## Open findings: 12 → 8 open + 2 awaiting your ruling

Closed this session: C17, C18, C19, C21, plus round 3's R1-R4.
Still open from §2: C5, C6, C7, C20, C10, C11, C13, C14, C15, C22
(C22 partially addressed — see §7 of the round-3 report).
Owner rulings owed: C12's layering, C18's catalog shape / R9's dead line,
whether C22 changes the C1 test, and now P1 and P2 above.


---

# Fourth addendum — round 4, the refutation pass

**HEAD `24b897ba4`. Gate green, tree clean.**

Round 3's debt is paid: its verdicts were the author's own traces, and
this is the out-of-family refutation pass they were owed.

- **R1, R2, R3 SURVIVED both readers independently** — first unanimous
  survival this audit has produced.
- **R4 was refuted by both, and both refutations were wrong** — each
  assumed the sub-mode toggle over an owned base enters the ALPHA
  acquire path; it retargets in place (probe: `ownsSlot0=1`).
- **But their failure mode is real.** Mutation M-A (revert R4's register
  asymmetry) reddens five assertions, two in the readers' own words.
  They found a real defect and located it in the wrong version of the
  code. **A refuted finding is not a worthless one — ask "which version
  is this true of" before discarding it.**

**Two things landed.** A hardening in `_forthConsoleAcquireRow` (retarget
an existing owned frame rather than stack a second), which is
**explicitly NOT mutation-provable** — M-B removes it and the gate stays
green; third documented gap of the stage. And
`test_console_ownership_invariant`, which asserts the ownership rule
after every step of two gesture sweeps via a new selftest-only stamp
census, because round 3's R4 was a documentation defect that survived a
whole session for want of a test. *An invariant that lives only in prose
is not enforcement.*

## Where round 5 starts

**Not with new coverage.** The in-family half is what is missing: the
`design` (D7) dimension — the one `CODE_AUDIT.md` calls the lens that
pays for the exercise — **has not run for two rounds** (credits killed
it in round 3, and round 4 was verification-only). Run the workflow's
dimensions again, D7 first.

The exit criterion needs **two consecutive** clean rounds and round 4
produced new code, so it cannot be met before round 6 at the earliest.

## Open findings: unchanged at 8, plus 2 awaiting your ruling

Nothing new confirmed this round. C5, C6, C7, C20, C10, C11, C13, C14,
C15, C22 still open; rulings owed on C12's layering, C18's catalog shape
/ R9's dead line, whether C22 changes the C1 test, and P1/P2 from round
3. The fold/suspend window still has no TAM-driven interactive fixture —
that gap is now three rounds old and is where I would expect the next
real finding.


---

# Fifth addendum — round 5, and the branch is pushed

**HEAD `ff9274f9f` at audit time. Gate green, tree clean.
`forth-core/stage-n` is now ON `origin`** — 99 commits, tracking set. The
standing "merge/push" item in §7 is half done: pushed, not merged.
`stage-k`, `-l`, `-m` and local `main` are all ancestors of `stage-n`, so
that one push carried everything.

## Round 5 ran the way round 4 asked, and it is the biggest round yet

`AUDIT_round5_2026-08-06.md`. All eight dimensions, **D7 included** — its
first run since round 2, which is what the fourth addendum demanded. 29
findings raised, 24 through the three-lens refutation, **17 survived**; §3
carries twelve numbered (R1–R12), plus two PLAUSIBLE, five past the
verification cap, and R13 from the out-of-family pass.

**Read R1 and R2 first.** An ordinary cancel — FORTH, type a line, f+6, SF,
SYS.FL, EXIT — bypasses the only `forthFoldUnwindIfDone` in `fnKeyExit`,
loses the typed line, and leaves the fold **ARMED**, after which a later
normal-mode `STO 05` records a program step instead of storing to R05. It
crosses modes, it is durable, and it is invisible until it bites.

**The round's centre is one state**, reached by three unrelated routes (R3
the f long-press, R7 the fold from alpha input, R8 stacked rows): *an
interactive capture open with no frame registered.* In it, both C18 gates
commit a `keysMode` flip that `forthConsoleShowSurface` then refuses to
follow — **C18's exact symptom, produced by C18's fix.** Three finders
found three doors independently; the agreement is worth more than any one
door.

**Fifth round, same pattern.** Nine of twelve confirmed findings are in
code this range wrote, or in pre-existing lines its fixes made harmful.
Six of them are about *who is allowed to write the frame* — C17 relocated
ownership into `softmenuStack[].userMenuId` correctly, and the standing
note holds: **relocating state is the most dangerous fix shape.**

## The out-of-family pass ran, and its value was re-ranking

Gemini 3.1 Pro, three packets, model probe passed on all three. §9 of the
report. It found no new surface. What it did was **change the weight of two
findings the in-family readers had already filed low**:

- **R13** — rung 3's safety argument cites "rung 2's pre-normalisation", a
  mechanism `b96ae1b33` deleted, at a rung number C18 changed. The
  conclusion still holds by an unstated invariant. An in-family reader had
  this under "documentation drift, noted not flagged"; two families and
  R11's precedent promoted it.
- **P-B/G2** — both landed on `forth_menu.c:528-535`, the `theirs` scan
  plus `RegisterSlot0(!theirs)`, with two different consequences. That
  makes **three independent out-of-family readers on one function's one
  shape** across rounds 4 and 5. Round 4 hardened the OWNED arm on exactly
  that agreement argument; **the BORROWED arm four lines below was left,
  and it is the arm both readers land on.** No test exercises `theirs`
  (§6.2), and R9 proves its registration is deletable with the gate green.

## Two process defects this round earned, both now fixed in the tooling

- **Mutation evidence was contaminated.** Verifiers mutate concurrently in
  the owner's shared tree; two of three runs were poisoned by a sibling's
  live `/* MUTATION */` edits, one seeing a baseline gate come back RED at
  a clean HEAD. Only the one reader that made its own worktree (R9)
  produced clean evidence. `audit-workflow.js` now passes
  `isolation: 'worktree'` to every refutation agent, and the brief tells
  readers not to revert a foreign edit.
- **Two packet defects, both mine, both in the orientation.** Condensing a
  load-bearing comment is truncation — a paraphrase dropped the clause
  naming `forthConsoleRestoreSurface` and the reader's whole finding rested
  on the gap. And a packet must state what ESTABLISHES a state, not only
  what it means: neither packet said the open borrows only when the current
  menu is already FWRD, so both of Gemini's findings assumed otherwise.
  Fourth and fifth packet artefacts in five rounds; both rules are now in
  `CODE_AUDIT.md`. Also recorded there: **9.6 KB and 10.9 KB packets were
  answered in minutes**, so the ~3 KB ceiling was too conservative.

## Exit criterion: NOT met, and further off than round 4

The round is not clean, so the count resets. **Earliest close is round 7.**

## Open findings: 12 → 25 open, plus 5 unverified and 6 rulings owed

New: R1–R13, P-A, P-B, U1–U5. Still open from before: C5, C6, C7, C10, C11,
C13, C14, C15, C20, C22 — none re-reported. Rulings owed: C12's layering,
C18's catalog shape / R9's dead line, whether C22 changes the C1 test, P1,
P2, and now R12.

## Where round 6 starts

**The TAM-driven interactive fixture** (R7's class test). R1, R2, R4, R7,
P-A and U1/U4 all live inside that window, U1 says round 4's own oracle
fires falsely at its first step, and the gap is now four rounds old — the
fourth addendum predicted the next real finding would come from there, and
R1/R2/R7 are it. Then the out-of-family pass over the same window, with the
functions inline. **Not with new coverage.**

Nothing was run in the simulator this round. Three findings (R3, R7, R8)
end in the same on-screen claim — the row says `A` while the keypad types
keys — and one `run-sim` capture would settle all three. Round 3's C21 was
found by a screenshot. That is the cheapest confirmation this round left on
the table.


---

# Sixth addendum — round 6, the fixture, and a PLAUSIBLE made a crash

**HEAD `24bd4db99` (unchanged — no code landed this round). Gate green, tree
clean. Round 6 is verification and findings only.** Report:
`AUDIT_round6_2026-08-08.md` (F1–F13, eight sections + a §9 the process
earned).

## Round 6 did what the fifth addendum asked: it DROVE the window

The fold/suspend fixture the handoff named for four rounds got built — as a
TAM-driven self-test run through the real gate in a worktree (F0–F4). It is
the round's whole yield, and it turned static traces into executed facts:

- **P-A → F1, a live crash.** Round 5's sole PLAUSIBLE is now a reproduced
  **SIGSEGV** with a product-code-only backtrace, reachable in **three
  keypresses from an open console: `GTO . .`**. The `.` promotes the armed
  fold's `tam.function` to GTOP, which runs LIVE, grows program memory, and
  moves `currentProgramNumber` off FHIST; the resume splice's unclamped
  `uint16_t n` (`manage.c:1335`) then deletes past the program END. **The
  out-of-family reader named the WRONG door** (the UP softkey, refuted —
  it keeps the cursor on FHIST); the fixture found the right one. A real
  finding through a different door than claimed, in the confirming
  direction — twice this round (see F11).
- **R1, R2, R7(a), R4 → EXECUTED** (F2, F3, F5, F6), R3 → **screenshot**
  (F7). R1's full durable cost is now observed, not argued: after the SYSFL
  cancel a normal-mode `STO 05` **stored nothing** (R05 kept 555) because
  the stranded armed fold forged it into a program step.
- **U1 → SETTLED TRUE** (F12), mutation-proven: the round-4 ownership oracle
  false-fires on `FCAP_SUSPENDED`. **This is the round-7 gate item** — it
  BLOCKS the fixture that pins F1–F9, so it is fixed first, as a
  precondition, not deferred.

## The bracket's teardown-owner set is open (D7-1), and F4 is the second door

Sol's design review (self-contained packet) ruled the fold bracket **cannot
be correct by construction while teardown and fold-finalisation have
separate owners**. F4 (S1-C) proves it: `GTO → . → PROG`-softkey strands the
same armed fold at `keyboard.c:1219`, a second site of R1's class, probe-
confirmed. The class census names nine more `leaveTamModeIfEnabled` callers
to check. Sol's `tamFinish` single-terminal-transition is the by-
construction fix and is an **owner design decision**, not an implementer
patch — it touches `ui/tam.c`'s eleven leave-then-dispatch sites.

## Open findings: 13 (F1–F13) + one doc item

**Confirmed (8 verified + the executed set):** F1 (crash), F2/F3/F4 (the
armed-fold class, three doors), F5/F6/F7 (row/mode + name clobber),
F8/F9 (the zombie-console cluster — R1's residue read as live at six
origin-vs-openness sites), F10 (near-cap TAM commit silently dropped),
F11 (EMIT/`.$` spill-settle clobbers a live register — **outside the
window**, from the accidental broad pass, re-verified), F12 (U1 oracle),
F13 (U5 inline EXIT ladder — rebase hygiene, no runtime path, **leave alone
for correctness**). Plus **D7-3**: the "PEM-only / inert in production"
comments on `manage.c:1305/1310/1895` are STALE and mislead every reader of
the crash path — a documentation fix.

**Refuted (5), do not re-report:** U2 (unconstructable wedge), U3
(banner names the read; doc nitpick), U4 (duplicate of R8+R2), G-W1a-1
(the orphan-drop is design-anticipated, its own cited comment rules it),
G-W2-1 (the UP door for F1's crash — wrong door).

**Not re-examined (out of window), still open from before:** C5, C6, C7,
C10, C11, C13, C14, C15, C20, C22. **Rulings owed, carried forward:** C12
layering, C18 catalog / R9 dead line, C22-vs-C1, P1, P2, R12 — and now
F10's keep-vs-sweep disposition collision, F13's extract-or-leave, and the
D7-1 `tamFinish` restructuring.

## Where round 7 starts

1. **F12/U1 first** — fix the oracle (`forthCapIsOpen() ||
   forthCapIsSuspended()`), because it false-fires at the suspend step of
   every TAM fixture and blocks pinning F1–F9.
2. **Land the fixture.** F0–F4 exist only in a disposable worktree. The
   TAM-driven battery is the four-round-old gap; land it as permanent
   coverage (each subcase asserting it REACHED its state), with F12 fixed so
   it does not false-fire.
3. **The armed-fold class (F1–F4).** Weigh Sol's `tamFinish` by-construction
   fix against three point-guards; relocating the teardown owner is the
   dangerous-fix shape, so whatever lands gets the census as its class test.
4. Then the row/mode findings (F5–F9) and the narrow silent-loss pair
   (F10/F11).

**Exit criterion: further from met than round 5.** A confirmed crash and a
second door reset the count; **earliest close is unchanged at round 7 at the
very earliest, and only if round 7 lands no new code** — which a fix wave
this size will not. Realistically the first clean pair is later.

## Process (the round earned two runner traps — see report §9)

Both caught before they reached the report, both now guarded in
`audit-workflow.js`: (1) isolated worktrees spawn at a STALE ref
(`c3a00768c`, ~114 behind) — every verifier now checks out to tip first,
proven by all 13 recording the checkout; (2) `resumeFromRunId` DROPS args,
which silently ran the wrong audit once — never resume a parameterized run,
relaunch fresh. Also: packet size ceiling raised to ~17 KB, the `MODEL:`
line now demanded in repo-hybrid packets' final message, and the workflow
renamed off the security-flavoured word that tripped a session guardrail.
The `cross-model-audit` skill is the operator's entry point and carries all
of it.


---

# Seventh addendum — 2026-08-08, the round-6 fix wave

**Gate GREEN, tree clean after the fix commit. Round 6's F1–F12 and D7-3
are FIXED and landed; F13 and the design rulings are the owner's.**

## What landed

One commit: every confirmed finding of `AUDIT_round6_2026-08-08.md` except
F13, each with a reproducer RED on the unfixed tree first (the red log is
quoted in the commit message), the classes named in DESIGN-HISTORY, and the
TAM-driven interactive fixture the last four rounds demanded —
`test_fold_round6_window`, nine subcases driving the real gesture chain
(fnForthOuter → runFunction → tamProcessInput → fnKeyExit/fnKeyEnter, the
timer exec chain for the long-press), plus suspend/resume steps in the
ownership sweep now that F12's oracle accepts a suspended stamp.

The headline mechanics: the resume splice re-anchors with
`defineCurrentProgramFromCurrentStep()` and clamps (F1, the `GTO . .`
SIGSEGV); the `.` promotion re-derives fold admission (GTOP → PARK); every
TAM teardown outside the two unwind owners calls the self-guarded
`forthFoldUnwindIfDone()` (F2/F4 and the census); a new
`forthCapInteractiveLive()` predicate answers "is there a live line" at
ten sites that were asking "whose capture" (F3/F6/F8/F9), and EXIT on a
suspended residue now RECOVERS the line; resume re-registers its row
through `forthConsoleRestoreSurface()` (F5); the long-press leaves a live
console's row alone (F7); `_forthFoldKeptSteps` reconciles the splice's
"keep" with the sweep (F10, with the old-contract subcase migrated);
`forthPrimInvoke` un-applies a refused prim's declared effect instead of
settling the spill against it (F11).

**RULE-1:** flash 1114632 → 1114904 = +272 B; ram 8884 unchanged; arena
untouched. `nm | grep -c forthConsole` = 19.

## Owner decisions still open

- **F13/U5** — extract the 135-line interactive EXIT ladder from upstream's
  `fnKeyExit` to a package seam. Rebase hygiene, no runtime path; round 6's
  own verdict says it should not gate a fix wave.
- **D7-1** — Sol's `tamFinish` single-owner redesign. The per-door unwinds
  above close the known strands; the by-construction fix is a design
  decision with its cost in ui/tam.c's eleven leave-then-dispatch sites.
- Carried from earlier rounds: C12's layering, C18's catalog shape / R9's
  dead line, whether C22 changes the C1 test, P1, P2, R12.

## Where round 7 starts

**On this commit.** Thirteen fixes in one day is the largest same-day fix
surface this audit has ever produced, and the regression record (r2 4/7,
r3 4/4, r5 9/12) says the next round's findings live in it. Audit the fix
commit first — D7 included — before any new coverage. The exit criterion
reset again; earliest close is now round 8. The out-of-family pass goes
through the `cross-model-audit` skill (packets from the template, linted,
dispatched with the identity check).


---

# Eighth addendum — 2026-08-08, rulings session

Owner ruled on the open queue (his test: a documented finding whose fix is
quick AND robust gets fixed; the rest need their documentation justified).

- **R12 and C12: FIXED, red-first**, same session — see DESIGN-HISTORY's
  rulings entry. C12's ruling: the VIEW owns the clamp
  (`forthConsoleRollView`, screen.c). RULE-1: +136 B flash, ram unchanged.
- **P2 stays a ruling** (matches native push — fixing would diverge from
  the platform), **P1 stays documented** (the quick fix double-pops the
  ladder and fires inside save/restore; robust version is a close-site
  sweep, schedulable as its own packet), **C22 stays an evidence note**
  (the code fix is right; only the test's device proof leaned on a
  sim-only flag).
- **D7-1: design drafted for review** —
  `DESIGN_D7-1_tamFinish_2026-08-08.md`. The compiler closes the external
  teardown set (leave goes static, `tamFinish` is the one public
  teardown); the eleven internal sites stay on the epilogue per the rev-2
  lesson. Lands AFTER round 7 reads the round-6 wave.
- **F13**: deferred to a rebase-adjacent stage, per the round-6 verdict.
- **Round 7: NEXT SESSION**, auditing `c701aa72b` (and now this commit)
  first. **Push is blocked on auth** — `git push` fails "Invalid username
  or token" on the https remote; the owner pushes or refreshes the
  credential. Branch is ahead of origin by three commits.
