# Handoff — 2026-08-06

Where `forth-core/stage-n` stands at the end of the Stage N + audit
session, what is open, and what the next session should not have to
rediscover. Non-normative; `DESIGN.md` is authoritative.

**HEAD `b8e1da153`, 89 commits ahead of `main`. Gate green, tree clean.**

---

## 1. What landed

**Stage N — the console (`84da2ff9c..486574c78`, six packets).** The
interactive Forth capture now has a face: a transcript over the stack
area, an output channel, keys-first entry. Normative record is
`DESIGN.md` §8.4.4, with §8.4.1 / §8.4.2 / §1.3 / §5.4 amended; the
narrative is in `DESIGN-HISTORY.md`. Sim captures:
`forum/screenshots/stage-n-{1,2}-*.png`.

**The adversarial audit (`d49ff2916` onward).** A multi-reader bug hunt
adapted from `forum/`'s post-audit workflow — see §4.

**Four audit findings fixed**, each with a class test and a mutation
that reddens it: C1 (`3712ea3a9`), the C2/C3/C4/C8/C9 softmenu-row family
(`db5e5ca30`), a restore-path frame leak (`d2fcb401c`), C16
(`723361f58`).

**Cumulative numbers** (`make dmcp5r47 CUSTOM_PKG_RECONFIGURE=1`), Stage
M close → HEAD: flash 1111680 → 1114120 = **+2440 B**; ram 7844 → 8884 =
**+1040 B** (the 1024-byte view ring plus its state). Arena untouched
throughout — no stage in this range changes the dictionary.

---

## 2. Open findings

Two reports, both committed:
`AUDIT_stages-K-to-N_2026-08-06.md` (round 1, 15 confirmed) and
`AUDIT_Stage-N-console-audit-round-1-fixes_round2-2026-08-06.md`
(round 2, 7 confirmed). Each finding there carries its reaching input,
the contract it violates, and a suggested class test.

**Start here.** C17 is the only open finding that costs the owner
something *unrecoverable*.

| # | what | why it matters |
|---|---|---|
| **C17** | `forthConsoleShowSurface` retargets slot 0 on menu identity alone, never asking whether the console *owns* that frame | the owner's own FWRD frame is consumed, not merely displaced — another press does not get it back. **See §3.** |
| C18 | three callers commit the sub-mode flip, then call a function entitled to do nothing, so the row can lie about the input mode | the keypad types `Σ+` where the row says `A` |
| C19 | the ENTER error arm appends the message into a word's still-open output record | the error text can be ellipsized off the right edge |
| C5 | history recall with nothing to recall wipes the line being typed | lost work, ordinary gesture |
| C6 | `fnForthOuter` has no already-open guard — pressing FORTH inside the console discards the line with no history push | lost work |
| C7 | `.S` prints a constant `<4>` and never shows the top four levels of an 8-level stack | wrong output; N-R5 says "depth-prefixed picture of the live stack" |
| C20 | `EMIT` truncates an out-of-`int32` long integer into the accepted range | prints a wrong character instead of refusing |
| C10 | `EMIT` accepts codes whose low byte is `0x00`, writing a lone high byte into the ring | glyph corruption |
| C11 | the formatter's `dtString` arm truncates on a byte boundary | same orphan-byte class as C10 |
| C12 | the roll clamps at `count-1` rather than `count-rows` | scrolling back empties the transcript band. **Needs a ruling, not a patch — see §6.** |
| C21 | nothing in the battery can fail if the console stops suppressing the register paints | a whole feature is untested |
| C22 | the C1 class test's canaries guard a buffer no producer receives | **see §5 — this one downgrades work already done** |
| C13 | the `.`-stack-delta assertion reads a counter zeroed before it is read | worse error message, not a missing gate |
| C14 | the console's `editorTop` constants (128/67) are hand-copied from `showStringEdC47` | merge hazard; two `_Static_assert`s fix it |
| C15 | `DESIGN.md`'s override list omits `assign.c`, still names `error.c` | one documentation line |

---

## 3. C17 — read this before attempting it

Confirmed, reproduced by mutation, **and I tried to fix it and failed.**

**The defect.** Browse to FWRD through the CATALOG tree, so the *owner's*
FWRD frame is slot 0. Press FORTH: the open sees FWRD already current, so
it pushes nothing and records `homePushed = 0` — correctly meaning "slot 0
is not ours". Press f+XEQ: `forthConsoleShowSurface` takes the
`cur == -MNU_FORTH` branch and overwrites `softmenuStack[0].softmenuId`
with the ALPHA index, **without ever asking whether that frame is the
console's**. Then any line running `calcModeNormal()` (`XEQ 'CLSTK'`)
pops it, because that guard matches `-MNU_ALPHA`. EXIT, and the owner gets
the catalog level that was underneath.

Frame *count* is conserved, which is why the landed battery passes; frame
*identity* is not.

**The obvious fix is not sufficient.** Gating the retarget on
`forthCapHomePushed()` and pushing when the console does not own slot 0
**failed the two probe rows it was meant to close and also regressed
`[open, alpha, ENTER, EXIT]`**, which had been passing — the owner's row
came back as a TAM menu (`-1387`). Reverted; the tree is green without it.

I did not get to the bottom of that interaction. What I would try next:

- The push-when-not-owned path sets `homePushed = true`, after which the
  *return* leg retargets rather than pops. Trace both legs together
  rather than the outbound one alone.
- `forthConsoleRestoreSurface` runs on the ENTER path and has its own
  push-and-take-ownership branch. Two functions can now both claim
  ownership of a frame in one line's lifetime.
- Sol's framing, reached independently and worth starting from:
  *"menu ownership — not merely visible menu identity — controls the EXIT
  ladder."* The right model may be an explicit owned-frame **identity**
  (which slot, which menu) rather than a single ownership bit.

The reproducing fixture is in the round-2 report under C17: the owner
fixture is `showSoftmenu(-MNU_CATALOG); showSoftmenu(-MNU_FORTH);` and the
differential is that a plain `1 2 +` line passes where a `XEQ 'CLSTK'`
line fails.

---

## 4. The audit system

| file | what |
|---|---|
| `CODE_AUDIT.md` | the process: dimensions, reader pool, what NOT to flag, exit criterion, report template |
| `PROMPT_CODE_AUDIT.md` | the auditor brief + the refutation brief. Paste whole, or inline into a subagent prompt |
| `audit-workflow.js` | the runner: 8 blind finders → dedup → adversarial refutation → report |

Run it with `Workflow({scriptPath: 'design-docs/forth-core/audit-workflow.js', args: {subject, commits, files, date}})`.

**The out-of-family reader is Gemini**, and this is settled after twelve
runs:

```bash
agy --model gemini-3.1-pro-high --print-timeout 12m -p "$(cat packet.txt)"
```

- **Flag order decides which model answers.** `agy -p --model X` silently
  serves *Claude*. This was the order documented in `forum/DESIGN.md`, so
  forum audits run that way were Claude auditing Claude — corrected there
  too, with a standing rule to put "state your model name" at the top of
  every audit prompt and check the answer.
- **Send whole functions**, and state the orientation of any shared
  structure the reader cannot inspect (e.g. "slot 0 is the TOP"). Round 2
  produced two findings that were artifacts of a truncated packet and a
  missing orientation, and both looked exactly like good findings.
- **Small packets only.** ~3 KB works in minutes; a 13 KB packet returned
  nothing from either reader.
- **Sol via `codex` does not complete an audit.** The sandbox blocker is
  real (bubblewrap missing; `--dangerously-bypass-approvals-and-sandbox`
  clears it, owner-authorised for this WSL2 box) but the remaining
  problem is behavioural: it reads exhaustively and ignores every budget
  instruction. Full diagnosis in `CODE_AUDIT.md`.

**Exit criterion: unmet, and it will reset on every fix.** Two
consecutive rounds with no new confirmed finding, at least one
out-of-family. Round 2 is the reason to trust that rule — **four of its
seven findings were caused by round 1's fixes.**

---

## 5. Things that downgrade work already done

**C22 — the C1 class test is weaker than the session suggested.** The
DMCP target build carries **no `-fstack-protector`**. The mutation that
went red for C1 went red because of the *simulator's* protector, not
because the canary test caught it. On the device — where C1's consequence
is a reboot — that backstop does not exist. The C1 *fix* is right; the
test is not the proof it appeared to be.

**Three vacuous tests in one session, all mine, all caught by running the
mutation and none by the gate.**

1. A hammer invariant comparing a `uint8_t` against 255 — `-Wtype-limits`
   caught it.
2. The C1 class test built its short integer with `255 XEQ 'HEX'` — `HEX`
   is a `CAT_FNCT` item reached by *bare name*, so the line raised
   label-not-found, X stayed a long integer, and the arm under test was
   never reached.
3. The C16 class test performed the save/restore *in the test body* and
   asserted the bits survived — testing its own copy of the block.

The rule that caught all three, and the one to keep: **a fixture must
assert it reached the state it claims to test**, and **a fix is not
finished until the mutation reddens.** A green suite proves nothing about
a test you just wrote.

---

## 6. Decisions that are the owner's, not the implementer's

- **C12's layering.** The roll should clamp at `count - rows`, but `rows`
  is a view concept the ring module deliberately does not have. Either the
  ring learns about rows, or the view owns the clamp. Worth a ruling
  before a patch.
- **C18's catalog shape** and **R9's dead line** — the round-2 report's §7
  says it would leave both alone.
- **Whether C22 changes the C1 test.** It would have left it alone if the
  target build had a stack protector.

---

## 7. Standing items, unchanged

- **DM42n hardware pass** for stages L, M and N.
- **Merge/push** `forth-core/stage-k`, `-l`, `-m`, `-n` — 89 commits, all
  unmerged.
- **FIX-6B MR push** and the **leak-report filing** (`UPSTREAM_REPORTS_*.md`).

---

## 8. Suggested order for the next session

1. **C17** with fresh attention, starting from §3 rather than from the
   obvious fix.
2. **C18 and C19** — both are ordinary, both are on gestures an owner
   makes constantly.
3. **C21** — a whole feature with no test that can fail is worse than a
   bug with one.
4. **Round 3**, which the reset rule requires anyway. Gemini for the
   out-of-family pass, whole functions, small packets.
5. Then the DM42n pass and the merge, with a finding list short enough to
   be honest about.
