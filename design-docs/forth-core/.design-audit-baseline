# forth-core design-audit baseline — regenerated by design-audit.sh --accept
# Raising any of these is a deliberate act: say why in the commit message.
#
# 2026-08-09 (post-consolidation design audit): the two REVIEW counts the
# close-out deliberately left flagging are now accepted, after the judgement
# read the close-out was deferring to (DESIGN-HISTORY, this date).
#
#   MAX_OVERRIDE_FILES / MAX_ADDED_LINES unchanged from the close-out.
#   BASE_NO_FORTH 3 -> 8, measured AFTER check B learned that _tamLeave is
#     Forth content (D7-1's package-introduced rename; 20 sanctioned one-line
#     rewrites in ui/tam.c were drowning the check). The eight that remain,
#     each read on its merits: freeList.c (FIX-6B, deferred on the upstream
#     MR), the manage.c masked-low-byte comment and softmenus.c
#     testInitVariableSoftmenu (accepted since the first baseline), three
#     D7-1 provenance comments in keyboard.c, and two paired-brace artifacts
#     of the round-8 guard restructures (keyboardTweak.c, screen.c).
#   BASE_BIG_BLOCKS 16 -> 36. All 36 read: every block is either a seam arm
#     that must run at that exact point (determineItem/tamEnterMode/the EXIT
#     and BACKSPACE ladders), a comment-dominated call site whose code is a
#     few lines of package calls, the manage.c standing exception (2.1), the
#     freeList guard, or the config.c selftest trigger (re-tested this
#     audit). One residual candidate recorded, not counted: sanitize/catalog
#     helpers in manage.c are movable through P8's own wrappers — see the
#     DESIGN-HISTORY entry.
#
# 2026-08-11 (the refresh-sweep ruling): b5c4020af's blanket refresh had
# swept working-area drift into the package. Two whole new overrides
# (printing/print.c, testSuite/hal/lcd.c — LCD-capture scaffolding with no
# caller) were REMOVED, taking override files 21 -> 19; the five non-Forth
# hunks it added were ruled one at a time by the owner.
#
#   BASE_NO_FORTH 8 -> 11, and every one of the eleven now has a
#     disposition. The three new keeps: the softmenus.c TIMER-hint guard
#     (real uninitialized-coordinate draw, upstream's own convention at
#     showSoftkey, filed upstream), and testSuite.c's sized snprintf and
#     fgets loop (real defects in the harness this gate runs). screen.c's
#     byte = 0 was kept as a warning silencer with no defect behind it;
#     screen.c's fcol/frow initializer was REVERTED and filed instead
#     (UPSTREAM_REPORTS_getGlyphBounds_partial.md).
#   BASE_BIG_BLOCKS 36 -> 26, a measured drop, accepted as the new ceiling.
#   MAX_ADDED_LINES stays 1243, the ruled budget: --accept would have
#     tightened it to the measured 1078 and left no headroom nobody asked
#     to give up.
#   Churn is NOT baselineable and reads 2: the softmenus guard re-indents
#     the two lines it wraps. Catalogued in the upstream-diff-review
#     skill's deliberate-exceptions file; it goes to 0 when upstream takes
#     the fix.
MAX_OVERRIDE_FILES=19
MAX_ADDED_LINES=1243
BASE_NO_FORTH=11
BASE_BIG_BLOCKS=26
